# Core runtime dependencies for the Python star tracker scripts.
# Install with:
#   python -m pip install -r requirements.txt
#
# Notes:
# - The FPGA/HLS C++ parts of the project are not covered here; they require the
#   vendor toolchain (for example, Vitis/Vivado) separately.
# - If you want notebook support, also install Jupyter/IPykernel manually or add
#   a dev requirements file for those tools.

numpy>=1.24
matplotlib>=3.7
torch>=2.2
torchvision>=0.17
